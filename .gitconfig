[user]
	email = boris.indelman@wayve.ai
	name = Boris Indelman
[alias]
    s = status
	ss = status -sb
	f = fetch
	p = pull
	c = checkout
	cm = checkout main
	cb = checkout -b
	b = branch
	a = add
	aa = add -a
	au = add -u
	com = commit -m
	coma = commit -a -m
	ca = commit --amend
	cane = commit --amend --no-edit	
	poop = stash pop
	ss = status -s
	graph = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all	
[color]
	ui = auto
[help]
	autocorrect = 20