interface:
  display_name: "Checkpoint Inspector"
  short_description: "Inspect checkpoint licenses and runs"
  default_prompt: "Use $model-checkpoint-inspector to inspect checkpoint licenses and run history for this model."
